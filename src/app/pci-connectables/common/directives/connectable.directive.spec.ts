import { ConnectableDirective } from './connectable.directive';

describe('ConnectableDirective', () => {
  it('should create an instance', () => {
    const directive = new ConnectableDirective();
    expect(directive).toBeTruthy();
  });
});
